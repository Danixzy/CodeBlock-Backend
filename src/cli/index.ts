#!/usr/bin/env node

// CLI utility scripts
// Example: node src/cli/migrate.ts

console.log('CLI utility - Add your CLI scripts here');

// Example: Migration script
// import { db } from '../database/knex';
// db.migrate.latest().then(() => process.exit(0));
